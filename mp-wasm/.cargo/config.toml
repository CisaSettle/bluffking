# getrandom 0.3 requires BOTH the "wasm_js" feature AND this backend cfg on
# wasm32-unknown-unknown (https://docs.rs/getrandom/0.3/#webassembly-support).
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
